files:
    "/etc/cron.d/wpcron":
        mode: "000644"
        owner: root
        group: root
        content: |
            */1 * * * * wget -O /dev/null http://wp.comma.com.ua/wp-cron.php?doing_wp_cron


commands:
    remove_old_cron:
        command: "rm -f /etc/cron.d/*.bak"
